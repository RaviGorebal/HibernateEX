DROP TABLE STUDENT_ADDRESS_M2M;
DROP TABLE ADDRESS_M2M;
DROP TABLE STUDENT_M2M;

CREATE TABLE STUDENT_M2M
(
   STUDENT_ID      NUMBER(5),
   STUDENT_NAME    VARCHAR2(45 BYTE)    NOT NULL
);

CREATE TABLE ADDRESS_M2M
(
   ADDRESS_ID      NUMBER(5),
   ZIPCODE         VARCHAR2(45 BYTE)      NOT NULL
);

CREATE TABLE STUDENT_ADDRESS_M2M
(
   ADDRESS_ID      NUMBER(5) NOT NULL,
   STUDENT_ID	   NUMBER(5) NOT NULL 
);

ALTER TABLE STUDENT_M2M ADD (
CONSTRAINT PK_STUDENT_M2M
PRIMARY KEY
(STUDENT_ID));

ALTER TABLE ADDRESS_M2M ADD (
CONSTRAINT PK_ADDRESS_M2M
PRIMARY KEY
(ADDRESS_ID));

ALTER TABLE STUDENT_ADDRESS_M2M ADD (
CONSTRAINT FK_STUDENT_ADD_M2M_STUDENT_ID
FOREIGN KEY (STUDENT_ID) 
REFERENCES STUDENT_M2M (STUDENT_ID));

ALTER TABLE STUDENT_ADDRESS_M2M ADD (
CONSTRAINT FK_STUDENT_ADD_M2M_ADD_ID
FOREIGN KEY (ADDRESS_ID) 
REFERENCES ADDRESS_M2M (ADDRESS_ID));